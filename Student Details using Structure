#include <stdio.h>

struct student {
    int roll;
    char name[20];
    float marks;
};

int main() {
    struct student s;
    scanf("%d %s %f", &s.roll, s.name, &s.marks);
    printf("%d %s %.2f", s.roll, s.name, s.marks);
}