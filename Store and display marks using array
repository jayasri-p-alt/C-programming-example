#include <stdio.h>

int main() {
    int i, n;
    int marks[50];

    printf("Enter number of students: ");
    scanf("%d", &n);

    for (i = 0; i < n; i++) {
        printf("Enter marks of student %d: ", i + 1);
        scanf("%d", &marks[i]);
    }

    printf("Marks:\n");
    for (i = 0; i < n; i++)
        printf("%d\n", marks[i]);

    return 0;
}