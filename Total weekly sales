#include <stdio.h>

int main() {
    int i;
    float sales, total = 0;

    for (i = 1; i <= 7; i++) {
        printf("Enter sales of day %d: ", i);
        scanf("%f", &sales);
        total += sales;
    }

    printf("Total Weekly Sales = %.2f\n", total);
    return 0;
}