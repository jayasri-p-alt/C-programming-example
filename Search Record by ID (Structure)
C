#include <stdio.h>

struct student {
    int id;
    char name[20];
};

int main() {
    struct student s[10];
    int n,i,search;
    scanf("%d",&n);

    for(i=0;i<n;i++)
        scanf("%d %s",&s[i].id,s[i].name);

    scanf("%d",&search);

    for(i=0;i<n;i++){
        if(s[i].id == search){
            printf("%d %s",s[i].id,s[i].name);
        }
    }
}