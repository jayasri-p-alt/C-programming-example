#include <stdio.h>

int main() {
    int i;
    float rain, total = 0;

    for (i = 1; i <= 12; i++) {
        printf("Enter rainfall of month %d: ", i);
        scanf("%f", &rain);
        total += rain;
    }

    printf("Average Rainfall = %.2f\n", total / 12);
    return 0;
}