#include <stdio.h>

int main() {
    int i;
    float expense, total = 0;

    for (i = 1; i <= 5; i++) {
        printf("Enter expense %d: ", i);
        scanf("%f", &expense);
        total += expense;
    }

    printf("Total Monthly Expenses = %.2f\n", total);
    return 0;
}