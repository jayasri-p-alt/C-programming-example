#include <stdio.h>

int main() {
    int i, n, marks[50], max;

    printf("Enter number of students: ");
    scanf("%d", &n);

    for (i = 0; i < n; i++)
        scanf("%d", &marks[i]);

    max = marks[0];
    for (i = 1; i < n; i++)
        if (marks[i] > max)
            max = marks[i];

    printf("Topper Marks = %d\n", max);
    return 0;
}