#include <stdio.h>

float bill(float price, int qty) {
    return price * qty;
}

int main() {
    float price;
    int qty;
    printf("Enter price and quantity: ");
    scanf("%f %d", &price, &qty);
    printf("Total Bill = %.2f", bill(price, qty));
    return 0;
}