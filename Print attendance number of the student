#include <stdio.h>

int main() {
    int i, n;

    printf("Enter number of students: ");
    scanf("%d", &n);

    printf("Attendance numbers:\n");
    for (i = 1; i <= n; i++) {
        printf("Student %d present\n", i);
    }

    return 0;
}