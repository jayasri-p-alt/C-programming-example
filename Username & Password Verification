#include <stdio.h>
#include <string.h>

int main() {
    char u[20]="admin", p[20]="1234";
    char user[20], pass[20];

    scanf("%s %s", user, pass);

    if(strcmp(u,user)==0 && strcmp(p,pass)==0)
        printf("Login Success");
    else
        printf("Invalid Login");
}